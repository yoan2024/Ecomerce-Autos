import React from "react";
import CarCard from "./CarCard";

const FeaturedCars = () => {
  return (
    <div>
      <div className="text-white font-semibold text-2xl">FEATURE CARDS</div>
      <CarCard />
    </div>
  );
};

export default FeaturedCars;
